import SistemskeSpremenljivke from "@entities/SistemskeSpremenljivke"

import { CreateSistemskeSpremenljivkeStub } from "@stubs/CreateSistemskeSpremenljivke.stub"

describe("SistemskeSpremenljivke", () => {
  describe("constructor", () => {
    it.todo(
      "should create an instance of SistemskeSpremenljivke with string id and date"
    )
    it.todo("should create an instance of SistemskeSpremenljivke")
  })

  describe("createInstance", () => {
    it.todo("should create an instance of SistemskeSpremenljivke")
  })

  describe("fetchSistemskeSpremenljivke", () => {
    it.todo(
      "should return a single instance of SistemskeSpremenljivke from the database"
    )
  })

  describe("insertSistemskeSpremenljivke", () => {
    it.todo("should insert a single instance of SistemskeSpremenljivke")
  })

  describe("updateSistemskeSpremenljivke", () => {
    it.todo(
      "should update a single instance of SistemskeSpremenljivke in the database"
    )
  })
})
