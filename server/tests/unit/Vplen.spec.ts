import VplenModel from "@models/vplenModel"
import type { IVplen, IVplenDetails } from "@shared/types"
import mongoose from "mongoose"

describe("Vplen", () => {
  describe("constructor", () => {
    it.todo("should create a new instance of Vplen")
  })

  describe("fetchVpleni", () => {
    it.todo(
      "should fetch an array of Vpleni for a specific user and page number"
    )
  })

  describe("fetchVpleniDatun", () => {
    it.todo("should fetch an array of Vpleni for a specific user and date")
  })

  describe("vnesiVplen", () => {
    it.todo("should create a new instance of Vplen and return it if successful")
  })

  describe("izbrisiVplen", () => {
    it.todo("should delete an instance of Vplen and return it if successful")
  })
})
